# Feature: format-date

## Summary
Add a simple function to format a date as a string.

## Technical Approach
Create `src/format-date.js` exporting a single function `formatDate(date)` that returns `date.toISOString()`.
There are no dependencies.

## Acceptance Criteria
- `src/format-date.js` exists and exports the function.

## Validation
Once complete, ensure you signal completion by running:
`aigon agent-status implementation-complete`
